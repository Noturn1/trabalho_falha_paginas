#include <stdio.h>

int main(){
    int val1, val2, soma;

    printf("Digite dois valores a serem somados: ");
    scanf("%d%d",&val1,&val2);

    // soma = val1 + val2;

    printf("O resultado da soma é: %i", (val1+ val2));
}
